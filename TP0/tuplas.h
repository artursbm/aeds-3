#ifndef _H_TUPLAS
#define _H_TUPLAS

//estrutura que guarda elementos de cada nó da árvore
//representa a tupla (min, max, sum) de um intervalo
//[i1,i2] dado dentro do vetor
struct elemento {
    int min;
    int max;
    int sum;
};

#endif